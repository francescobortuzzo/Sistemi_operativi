#include <stdio.h>
#include <string.h>

// restituisce il puntatore della posizione di c in una stringa

int main(){
	
	int string;
	printf("inserisci la stringa: ");
	scanf("%s", &string);
	printf("inserisci il carattere da ricercare: ");
	scanf("%c", &c);	
	*strchr(char string, char c);
	printf ("il carattere %c si trova in posizione %c\n", c strchr);
}

void char *strchr(char *str, char c){
	int n = 0;
	int *count = 
	while()
	
}
